# Claude Code Tool Notes

In Claude Code, use the `Skill` tool to load a skill. When invoked, the skill
content is loaded and presented directly; follow it as written.

Never use the file read tool to load skill files when the `Skill` tool is
available.
